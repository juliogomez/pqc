# Python side. Install into a virtualenv:
#   python3 -m venv venv && ./venv/bin/pip install -r requirements.txt
#
# ncclient is what ansible.netcommon's netconf connection uses under the hood.
# paramiko backs the network_cli connection.
ansible-core>=2.18
ncclient>=0.6.15
paramiko>=3.4
lxml>=5.0
